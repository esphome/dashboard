#!/usr/bin/env bash

# Stop on errors
set -e

cd "$(dirname "$0")/.."

if [ ! -d "./node_modules" ]; then
    echo "Directory /node_modules DOES NOT exists."
    echo "Running npm install"
    npm install
fi

# Use rollup like upstream - it properly handles Monaco editor bundling
NODE_ENV=production npm exec -- rollup -c
