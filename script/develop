# Stop on errors
set -e

cd "$(dirname "$0")/.."

rm -rf esphome_frontend

mkdir esphome_frontend
cp -r static esphome_frontend/static

# Quit all background tasks when script exits
trap "kill 0" EXIT

npm exec -- serve --cors -l 8500 &
npm exec -- rollup -c --watch &
wait
